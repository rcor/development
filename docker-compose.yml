version: "3.9"  # optional since v1.27.0
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ~/.aws:/home/term/.aws
      - ~/Documents:/home/term/data
volumes:
  logvolume01: {} 